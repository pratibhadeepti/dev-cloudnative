# Global defaults, applied to all receivers where not explicitly overridden. Optional.
defaults:
  # API access fields.
  api_url: https://cloudibility.atlassian.net
  user: cnva-feedback@cloudibility.io
  password: 'Cloud@4321'

  # The type of JIRA issue to create. Required.
  issue_type: Bug
  # Issue priority (optional)
  priority: Low
  # Go template invocation for generating the summary. Required.
  summary: '{{ template "jira.summary" . }}'
  # Go template invocation for generating the description. Optional.
  description: '{{ template "jira.description" . }}'
  # State to transition into when reopening a closed issue. Required.
  reopen_state: "To Do"
  # Do not reopen issues with this resolution. Optional.
  wont_fix_resolution: "Won't Fix"

# Receiver definitions. At least one must be defined.
receivers:
    # Must match the Alertmanager receiver name. Required.
  - name: 'jira_alert'
    # JIRA project to create the issue in. Required.
    project: CNVAINT
    # Copy all Proetheus labels into separate JIRA labels.
    add_group_labels: false

#  - name: 'jira-xy'
#    project: XY
#    issue_type: Task
    # JIRA components. Optional.
#    components: [ '' ]
    # Standard or custom field values to set on created issue. Optional.
    #
    # See https://developer.atlassian.com/server/jira/platform/jira-rest-api-examples/#setting-custom-field-data-for-other-field-types for further examples.
    fields:
      # TextField
  #    customfield_10001: "Random text"
      # SelectList
  #    customfield_10002: { "value": "red" }
      # MultiSelect
  #    customfield_10003: [{"value": "red" }, {"value": "blue" }, {"value": "green" }]

# File containing template definitions. Required.
template: jiralert.tmpl
